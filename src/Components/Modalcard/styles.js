import  { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    root: {
      borderWidth: 1,
      borderColor: 'green',
      borderRadius: 10,
      backgroundColor: '#fff',
     
      
    },
    HeaderText:{ fontSize: 15,
      fontWeight: 'bold' },
  })